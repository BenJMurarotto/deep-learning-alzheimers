# Inference runtime for ViT Alzheimer's classifier (Python 3.10.x)
# Pins are loose except for the critical trio used during training.
# Note: TensorFlow 2.19 wheels target Python <=3.11. For Python 3.13+, use TF 2.20 instead.

# Core DL stack (trained on these majors/minors)
tensorflow~=2.19.0        # Python 3.10/3.11
keras~=3.11
tf-keras~=2.19
transformers~=4.52
huggingface-hub~=0.31

# Serialization / numerics
numpy>=1.26                # works with TF 2.19 (you used 2.0.1)
protobuf>=5.0.0,<6.0       # you used 5.29.3

# Imaging / viz
pillow>=10.0.0
opencv-python-headless>=4.10.0
scikit-image>=0.25.0
matplotlib>=3.10.0

# Data / ML utils
pandas>=2.3.0
scikit-learn>=1.6.0
scikit-optimize>=0.10.0
nibabel>=5.3.0

# UI
streamlit>=1.37.0          # your team’s UI choice


# macOS Apple Silicon note:
# If using Apple Silicon, replace `tensorflow` above with `tensorflow-macos`
# and (optionally) add `tensorflow-metal` for GPU acceleration.
